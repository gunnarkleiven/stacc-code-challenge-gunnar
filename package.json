{
  "name": "stacc-code-challenge-gunnar",
  "version": "1.0.0",
  "engines": {
    "node": "14.x"
  },
  "scripts": {
    "heroku-prebuild": "cd frontend/ && npm install && mnpm build && cd ..",
    "start": "cd frontend/ && npm start"
  }
}
